{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "echo \"(python build noop)\" && exit 0",
    "lint": "ruff check app",
    "typecheck": "mypy app"
  }
}
